# S3 Configuration for Dynamic Config Loading
# ============================================

# S3 bucket name where config.json is stored
S3_BUCKET=td-demo-configuration

# S3 object key (path to config file within bucket)
S3_CONFIG_KEY=config.json

# How often to refresh config from S3 (in minutes)
REFRESH_INTERVAL_MINUTES=15

# AWS region for S3 bucket
AWS_REGION=us-east-1

# Optional: API key for model endpoints (if required)
API_KEY=your-api-key-here

# AWS Credentials (for local development)
# ============================================
#
# Option 1: Export from AWS CLI profile
# Run: aws configure export-credentials --profile dev --format env
# Then source the output:
#   eval "$(aws configure export-credentials --profile dev --format env)"
#
# Option 2: Set manually (not recommended for security)
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
# AWS_SESSION_TOKEN=your-session-token-if-using-temporary-credentials
#
# Option 3: Use AWS CLI default profile (already configured via 'aws configure')
# The app will automatically use your default AWS profile if no env vars are set
#
# Note: In production, use IAM roles attached to ECS tasks or EC2 instances
